services:
  - type: web
    name: docanchor
    env: docker
    region: oregon
    plan: free
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    envVars:
      - key: USE_MOCKS
        value: "true"
      - key: QDRANT_IN_MEMORY
        value: "true"